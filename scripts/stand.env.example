# Copy to scripts/stand.env (gitignored). Overrides for run_stream.sh.
#
# Dev VM:
IFACE=enp0s3
EXPERIMENT_INI=cv_odmr.ini
RUN_LABEL=run
#
# Lab stand example:
# IFACE=enp6s0
# ODMR_ROOT="$HOME/odmr"
# EXPERIMENT_INI="$ODMR_ROOT/cv_odmr.ini"
# RUN_LABEL=production
#
# Optional CLI overrides (ini used when unset):
# REPEATS_PER_FREQ=100
# EXPECTED_GROUPS=36